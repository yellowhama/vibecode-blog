---
// /learn — The Agentic Software Workshop (S-grade course funnel).
// vibecode 의 강의형 승급: 블로그(무료 권위/AEO 깔때기) → 워크샵(전환).
// 커리큘럼은 실제 발행된 Field Notes 글에 1:1 그라운딩 — 발명 아님, 살아있는 교재.
// 결제 미연동 + 대외 가격 약속 리스크 → buy 버튼 대신 "Founding Cohort 대기자(뉴스레터 캡처)".
import { getCollection } from "astro:content";
import Layout from "@/layouts/Layout.astro";
import Header from "@/components/Header.astro";
import Footer from "@/components/Footer.astro";
import NewsletterSignup from "@/components/NewsletterSignup.astro";
import LiveEditor from "@/components/LiveEditor.tsx";

// 권위 증명: 실제 발행 글 수 = 살아있는 교재 규모.
const posts = await getCollection("blog", ({ data }) => !data.draft);
const noteCount = posts.length;

// 커리큘럼 — 각 모듈은 실제 Field Note 글에 그라운딩(slug 존재 시 링크).
const MODULES = [
  { n: "01", title: "The 1-Person Unicorn Stack", desc: "The complete agentic architecture to run a 10-person operation solo—what to automate and what to handhold.", slug: "1-person-unicorn-tech-stack-2026" },
  { n: "02", title: "Design as a Technical Contract", desc: "Locking down UI/UX through exact design tokens to eliminate agent hallucination in frontend code.", slug: "design-is-a-technical-contract" },
  { n: "03", title: "Agent Memory, Structured", desc: "Building persistent operational memory across sessions—knowing what to save and what to flush.", slug: "ai-memory-operating-structure" },
  { n: "04", title: "The Disk Contract for Agent Work", desc: "Setting strict filesystem boundaries to prevent agents from corrupting local dev environments.", slug: "ai-agent-work-disk-contract" },
  { n: "05", title: "MCP Shared State & Data Boundaries", desc: "Resolving data leaks when multiple stateless MCP servers interact with shared databases.", slug: "mcp-shared-state-data-leak" },
  { n: "06", title: "Evals: Self-Improving Agents", desc: "You can't improve what you don't measure. Building evaluation harnesses for self-healing loops.", slug: "self-improving-agents-need-evals" },
  { n: "07", title: "Frustration as Spec", desc: "Transforming developer frustration into strict prompt specifications for continuous refinement.", slug: "frustration-as-spec" },
  { n: "08", title: "Review Artifacts for Agents", desc: "Designing visual HTML sandbox artifacts that allow human operators to review agent work in 30 seconds.", slug: "html-review-artifacts-for-agents" },
];

const PILLARS = [
  { k: "Evidence, not hype", v: "No magic wand narratives. We teach exclusively through raw run logs, code, and live artifacts." },
  { k: "Live curriculum", v: `${noteCount} published Field Notes serve as a living textbook that constantly evolves with our production system.` },
  { k: "Copy the systems", v: "This isn't theory. Copy and paste our actual operating tech stacks, CI contracts, and agent harnesses." },
];

const FAQS = [
  { q: "Who is this workshop for?", a: "Senior engineers and solopreneurs looking to run actual products and companies with agents. If you are looking for simple prompt hacks, this is not for you." },
  { q: "How is this different from the blog?", a: "Field Notes are fragmented pieces of our operation. The workshop weaves these pieces into one cohesive, deployable operating system with step-by-step code and checklists." },
  { q: "Are there any prerequisites?", a: "You should be comfortable with production-level software engineering. More importantly, you must be able to think of systems as strict 'contracts'." },
  { q: "Pricing and schedule?", a: "Details regarding the Founding Cohort pricing and capacity will be sent exclusively to the waitlist before any public launch. Secure your spot now." },
];
---

<Layout
  title="Workshop | vibecode — Build a 10-person company, alone"
  description="The agentic software workshop. Copy the operating systems behind a 1-person company: stack, design contracts, agent memory, evals, and review artifacts. Grounded in real run logs, not hype."
>
  <Header />

  <main id="main-content" class="app-layout">
    <!-- HERO -->
    <section class="relative border-b border-border py-16 sm:py-24 overflow-hidden">
      <!-- Background Video -->
      <div class="absolute inset-0 z-0">
        <video 
          autoPlay 
          loop 
          muted 
          playsInline 
          class="w-full h-full object-cover opacity-30"
          src="/hero-bg.mp4"
        ></video>
        <div class="absolute inset-0 bg-gradient-to-b from-background/40 via-background/80 to-background z-10"></div>
      </div>

      <div class="relative z-20">
        <div class="inline-flex items-center gap-3 px-3 py-1 mb-8 text-[0.7rem] font-mono tracking-widest uppercase border rounded-full border-accent bg-accent/10 text-accent shadow-[0_0_10px_rgba(0,229,153,0.3)]">
          <span class="w-2 h-2 rounded-full bg-accent animate-pulse"></span>
          The Agentic Software Workshop
        </div>

        <h1 class="text-4xl sm:text-5xl lg:text-6xl font-bold tracking-tight leading-[1.05] max-w-4xl text-foreground">
          Build a 10-person company,
          <span class="text-transparent bg-clip-text bg-gradient-to-r from-accent to-accent-dim">alone.</span>
        </h1>

        <p class="mt-6 text-lg sm:text-xl text-text-secondary max-w-2xl leading-relaxed">
          Not just prompt tips. Copy the exact <strong class="text-foreground">operating system</strong>
          we use to run actual products with agents — stack, design contracts, agent memory, evaluation harnesses, and review artifacts.
          Every lesson is derived from real run.log files and code.
        </p>

        <div class="mt-10 flex flex-wrap gap-4">
          <a href="#join" class="s-btn-primary">Join the Founding Cohort →</a>
          <a href="/posts" class="s-btn-ghost">Read the free Field Notes</a>
        </div>

        <!-- proof strip -->
        <div class="mt-12 grid grid-cols-2 sm:grid-cols-4 gap-px bg-border border border-border rounded-md overflow-hidden backdrop-blur-md bg-background/50">
          <div class="p-4">
            <div class="font-mono text-2xl font-bold text-accent">{noteCount}</div>
            <div class="text-xs text-text-muted mt-1 uppercase tracking-wider">Live field notes</div>
          </div>
          <div class="p-4">
            <div class="font-mono text-2xl font-bold text-accent">8</div>
            <div class="text-xs text-text-muted mt-1 uppercase tracking-wider">Workshop modules</div>
          </div>
          <div class="p-4">
            <div class="font-mono text-2xl font-bold text-accent">100%</div>
            <div class="text-xs text-text-muted mt-1 uppercase tracking-wider">From real runs</div>
          </div>
          <div class="p-4">
            <div class="font-mono text-2xl font-bold text-accent">0</div>
            <div class="text-xs text-text-muted mt-1 uppercase tracking-wider">Magic wands</div>
          </div>
        </div>
      </div>
    </section>

    <!-- SNEAK PEEK (LIVE EDITOR) -->
    <section class="py-16 border-b border-border">
      <div class="mb-8 text-center">
        <div class="font-mono text-xs uppercase tracking-widest text-accent mb-2">Interactive Sneak Peek</div>
        <h2 class="text-3xl font-bold tracking-tight">Stop reading. Start fixing.</h2>
        <p class="mt-2 text-text-secondary max-w-xl mx-auto">
          In the workshop, you don't just watch videos. You fix failing Go daemon tests directly in the browser to advance. Try it below.
        </p>
      </div>
      <LiveEditor client:only="react" />
    </section>

    <!-- PILLARS -->
    <section class="py-16 border-b border-border">
      <h2 class="font-mono text-sm uppercase tracking-widest text-text-muted mb-8">Why this workshop</h2>
      <div class="grid gap-6 sm:grid-cols-3">
        {PILLARS.map((p) => (
          <div class="s-card p-6">
            <h3 class="text-lg font-bold text-foreground">{p.k}</h3>
            <p class="mt-2 text-text-secondary leading-relaxed">{p.v}</p>
          </div>
        ))}
      </div>
    </section>

    <!-- CURRICULUM -->
    <section class="py-16 border-b border-border">
      <div class="flex items-end justify-between flex-wrap gap-4 mb-8">
        <h2 class="text-3xl font-bold tracking-tight">The curriculum</h2>
        <p class="text-sm text-text-muted max-w-md">
          Each module starts from a publicly available Field Note. Read them for free first, then weave them into a working system in the workshop.
        </p>
      </div>

      <div class="grid gap-px bg-border border border-border rounded-md overflow-hidden">
        {MODULES.map((m) => (
          <a href={`/posts/${m.slug}`} class="group bg-background p-6 flex gap-5 items-start no-underline hover:bg-bg-secondary transition-colors">
            <span class="font-mono text-sm font-bold text-accent shrink-0 pt-1">{m.n}</span>
            <div>
              <h3 class="text-lg font-bold text-foreground group-hover:text-accent transition-colors">{m.title}</h3>
              <p class="mt-1 text-text-secondary leading-relaxed">{m.desc}</p>
              <span class="mt-2 inline-block text-xs font-mono text-text-muted group-hover:text-accent">read the field note →</span>
            </div>
          </a>
        ))}
      </div>
    </section>

    <!-- FUNNEL: free → workshop -->
    <section class="py-16 border-b border-border">
      <div class="s-card p-8 sm:p-10">
        <h2 class="text-2xl font-bold tracking-tight">From free notes to a working system</h2>
        <div class="mt-8 grid gap-6 sm:grid-cols-3 text-sm">
          <div>
            <div class="font-mono text-accent">01 — Read</div>
            <p class="mt-2 text-text-secondary leading-relaxed">Absorb the mindset and evidence through our free Field Notes first.</p>
          </div>
          <div>
            <div class="font-mono text-accent">02 — Build</div>
            <p class="mt-2 text-text-secondary leading-relaxed">Build the stack, contracts, memory, and evaluation harnesses step-by-step in the workshop.</p>
          </div>
          <div>
            <div class="font-mono text-accent">03 — Operate</div>
            <p class="mt-2 text-text-secondary leading-relaxed">Possess the operating system to run a 10-person operation completely solo.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- PRICING / FOUNDING COHORT -->
    <section id="join" class="py-16 border-b border-border">
      <div class="grid gap-8 lg:grid-cols-[1.2fr_1fr] items-start">
        <div class="s-card p-8">
          <div class="font-mono text-xs uppercase tracking-widest text-accent">Founding Cohort</div>
          <h2 class="mt-3 text-3xl font-bold tracking-tight">Save your seat first.</h2>
          <p class="mt-4 text-text-secondary leading-relaxed">
            We are opening the first cohort to a very small group. Capacity, schedule, and founding prices will be revealed <strong class="text-foreground">exclusively to the waitlist</strong> before going public. You are just saving your seat—no cost, no commitment.
          </p>
          <ul class="mt-6 space-y-2 text-sm text-text-secondary">
            <li class="flex gap-2"><span class="text-accent">✓</span> All 8 modules + raw code, harnesses, and checklists</li>
            <li class="flex gap-2"><span class="text-accent">✓</span> A living textbook updated as we operate (lifetime updates)</li>
            <li class="flex gap-2"><span class="text-accent">✓</span> Exclusive Founding Cohort pricing</li>
          </ul>
        </div>

        <div class="s-card p-8 bg-bg-secondary">
          <h3 class="text-lg font-bold text-foreground">Join the waitlist</h3>
          <p class="mt-1 text-sm text-text-secondary leading-relaxed">
            Be the first to know when the workshop opens. Field Notes will be sent to the same channel. Zero spam.
          </p>
          <NewsletterSignup class="mt-5" compact={true} />
        </div>
      </div>
    </section>

    <!-- FAQ -->
    <section class="py-16">
      <h2 class="text-3xl font-bold tracking-tight mb-8">FAQ</h2>
      <div class="space-y-4 max-w-3xl">
        {FAQS.map((f) => (
          <details class="s-card p-5 group">
            <summary class="cursor-pointer font-semibold text-foreground list-none flex justify-between items-center">
              {f.q}
              <span class="text-accent font-mono group-open:rotate-45 transition-transform">+</span>
            </summary>
            <p class="mt-3 text-text-secondary leading-relaxed">{f.a}</p>
          </details>
        ))}
      </div>
    </section>
  </main>

  <Footer />
</Layout>

<style>
  /* Techno-Futurist Theme */
  .s-card {
    border: 1px solid var(--border);
    border-radius: 0.5rem;
    background: var(--bg-secondary);
    transition: all 0.2s ease;
  }
  .s-card:hover {
    border-color: var(--border-strong);
    box-shadow: 0 0 15px var(--accent-glow);
  }
  
  .s-btn-primary {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.75rem 1.5rem;
    font-weight: 600;
    background: var(--accent);
    color: var(--background);
    border-radius: 0.375rem;
    transition: all 0.2s ease;
    text-decoration: none;
    box-shadow: 0 0 10px var(--accent-glow);
  }
  .s-btn-primary:hover {
    background: var(--accent-dim);
    box-shadow: 0 0 20px rgba(0, 229, 153, 0.4);
    transform: translateY(-1px);
  }
  
  .s-btn-ghost {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.75rem 1.5rem;
    font-weight: 600;
    border: 1px solid var(--border-strong);
    background: transparent;
    color: var(--foreground);
    border-radius: 0.375rem;
    transition: all 0.2s ease;
    text-decoration: none;
  }
  .s-btn-ghost:hover {
    border-color: var(--accent);
    color: var(--accent);
    background: var(--accent-glow);
  }
</style>
